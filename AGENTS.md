# Workspace Policy Bootstrap

Read and follow `../AGENTS.md` before doing any task work. Resolve the shared
policy paths from that parent workspace entry point. Stop if it or any required
shared file is missing.
